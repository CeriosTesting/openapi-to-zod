openapi: 3.0.0
info:
  title: No Operations Spec
  version: 1.0.0
paths:
  /users:
    # No operations defined
    description: Users endpoint with no operations
components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: string
