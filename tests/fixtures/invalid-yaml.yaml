openapi: 3.0.0
info:
  title: Invalid YAML Test
  version: 1.0.0
components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: string
        name
          type: string  # Missing colon - invalid YAML
