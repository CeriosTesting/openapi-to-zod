openapi: 3.0.0
info:
  title: Invalid Nested Ref Test
  version: 1.0.0
components:
  schemas:
    InvalidNested:
      type: object
      properties:
        metadata:
          type: object
          properties:
            settings:
              $ref: '#/components/schemas/NonExistentSettings'
